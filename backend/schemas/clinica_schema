from pydantic import BaseModel, Field, EmailStr
from typing import Optional
from datetime import datetime
from decimal import Decimal
from enum import Enum

class ClinicaBase(BaseModel):
    nome = str = field(min_length= 5, max_length = 50)
    endereco = str = field( min_length= 10, max_length = 150)
    telefone = str = field(min_length= 11 , max_length = 15)
    email = EmailStr = field(min_length= 15 , max_length = 50)
    senha = str = field(min_length= 8 , max_length = 100)


class clinicainput(ClinicaBase):
    pass

class ClinicaUpdate(BaseModel):
    nome: Optional[str] = field(min_length= 5, max_length = 50)
    endereco: optional[str] = field( min_length= 10, max_length = 150)
    telefone: Optional[str] = field(min_length= 11 , max_length = 15)
    email: optional[Emailstr] = field(min_length= 15 , max_length = 50)
    senha: optional[str] = field(min_length= 8 , max_length = 100)

    class clinicaresponse(clinicaBase):
        id:int

        model_confing ={
            'from_attributes': True
        }


